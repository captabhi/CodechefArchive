#include<iostream>
using namespace std;
int main()
{
	unsigned int n,max,maxx=100000,maxy=500;
	cin>>n;
	int arrx[n];
	int arry[n];
	arr1[0]=0;arr2[0]=0;
	for(i=1;i<=n;i++)
	{
		cin>>arrx[i];
		cin>>arry[i];
	}
	max=0;
	for(i=1;i<=n;i++)
	{
		if(arr[i]-arr[i-1]>max)
		max=arr[i]-arr[i-1];
	}
	if (maxx-arrx[n]>max)
	max=maxx-arrx[n];
	
	
	
}
